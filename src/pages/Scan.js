import React from 'react';

import Header from '../components/Header/Header';
import Scanner from '../components/Scanner/Scanner';

const Scan = () => {
    // const navigate = useNavigate();
    // const { setOpenedBook } = useContext();

    // const display = (data) => {
    //     useContext(data);
    // };

    return (
        <div>
            <Header />
            <Scanner />
        </div>
    );
};

export default Scan;
